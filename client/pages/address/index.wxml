<!--index.wxml-->
<view class="container">
    <!--list列表-->
    <view class="item-list-container">
        <block wx:for="{{list}}" wx:for-index="index" wx:key="index">
            <view class="item-container" bindtap="selectAddress" data-address="{{ item.storeAddress }}"
                  data-storemanagermame="{{item.storeManagerName}}" data-storephone="{{item.storePhone}}">
                <view class="item-info">
                    <text>店长：{{item.storeManagerName}}\t\t\t\t\t{{item.storePhone}}</text>
                    <text class="top">{{item.distance}}</text>
                </view>
                <view class="item-info">
                    <view class="address-des">
                        <image src="../../../assets/images/iconfont-addr-default.png" class="address-icon"/>
                        <text class="address-text">{{item.storeAddress}}</text>
                    </view>
                    <text class="item-suggest {{index!=0 ? 'hidden' :''}}">推荐</text>
                </view>
            </view>
        </block>
    </view>
</view>